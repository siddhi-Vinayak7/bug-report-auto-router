# bug-report-auto-router
Multi-label bug triage system that predicts module (Auth, Chat, Tasks, Profile, Payments, Other) and severity (Critical, Major, Minor) from free-text bug reports. Includes human-correction logging, held-out test set evaluation, and a full frontend + backend + DB deployment.
